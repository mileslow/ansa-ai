import{generateCompanyProfile}from'../lib/company-profile.js';export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{res.status(200).json(await generateCompanyProfile(req.body?.url,process.env.OPENAI_API_KEY))}catch(e){res.status(400).json({error:e.message||'Could not generate company profile'})}}
